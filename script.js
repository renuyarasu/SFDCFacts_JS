// JavaScript: Working with DOM

// getElementsBy

const button = document.querySelector("button");
// button.remove(); // remove button
// button.innerHTML = 'Delete';  // change button text
(button.style.backgroundColor = "tomato"), (button.style.color = "white"); // change button styles
console.log(button);
